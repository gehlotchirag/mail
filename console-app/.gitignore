node_modules/
.next/
.env*
*.env
*.pem
*.key
firebase-service-account.json

# TypeScript incremental build cache — regenerated by every build, and large
# enough to exceed the SSM payload cap that deploy.py ships files through.
tsconfig.tsbuildinfo
